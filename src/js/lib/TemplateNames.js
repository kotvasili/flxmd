export const TemplateNames = (template) => 
  `<div class="frame__name" slip-mouse-target="${template.id}">
		<div class="slide__element title-color">
			<span>${template.titlleList}</span>
		</a>
	</div>`;
